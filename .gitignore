node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example

# Agent skills (installed via `npx skills add`)
.agents/
.claude/skills/

# Local secrets — never commit
.supabase-secrets.local.sh
