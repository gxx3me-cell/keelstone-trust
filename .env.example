# Lumen deposit listener (BSC / USDT BEP-20)
# The listener is a long-running server — see Desktop/lumen-listener.
VITE_LISTENER_URL=http://localhost:3001
# Must match API_SECRET in the listener's .env
VITE_LISTENER_SECRET=
