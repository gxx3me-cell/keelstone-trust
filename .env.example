# The frontend needs no secrets.
#
# Crypto payments run through ChainFlow, accessed via CocoBase cloud functions —
# the ChainFlow secret key lives server-side in those functions, never in the browser.
# See README for the ChainFlow API key / webhook setup.
